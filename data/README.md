# Data

Large raw data archives are not included in this repository.

To reproduce the full workflow locally, place the MMASH archive here:

`data/raw/MMASH.zip`

The repository includes processed CSV files in:

`data/processed/`

External public datasets are stored in:

`data/external/`

Raw archives are ignored by Git to keep the repository lightweight.
